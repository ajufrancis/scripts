#!/usr/bin/env python

if no.ip and no.host
if itil.request == User_Acknowledged.Stopped
if state:
  Destroyed,Running,Stopped
if hyv == xs:
  chk_add('xstools')
  chk_xstools.state
if instancename.exists()
  alias = display name
if cpunumber:
  cpu.loads.warn.default = 
  cpu.speed
  memory
  publicip
  domain.user
  vm.zonename
  vm.account
  vm.host
  vm.mac
  vm.ip
  vm.svcoffername

created uptime

