#!/bin/bash
#test -f /usr/bin/salt || exit
#salt-key | grep -q $HOSTNAME || exit
#salt "$HOSTNAME" cmd.run 'xe vm-list params=name-label --minimal is-control-domain=false' --output text | tr ',' '\n'
#xe vm-list is-control-domain=false params=uuid,name-label,power-state,memory-actual,VCPUs-max,console-uuids,possible-hosts,HVM-boot-policy,HVM-boot-params,PV-bootloader,PV-bootloader-args,PV-args,dom-id,start-time,install-time,os-version,PV-drivers-version,PV-drivers-up-to-date,networks
